public class Main {
    public static void main(String[] args) {
        // write your code here
        while (true) {
            int a = (int) (Math.random() * 6) + 1;
            int b = (int) (Math.random() * 6) + 1;
            System.out.println("(" + a + "," + b + ")");
            if ((a+b) == 5){
                break;
            }
        }
    }
}

앞에 인트를 쓰는 이유는 math함수는 소수점까지 다 가지고 와서 int를 써줘야 정수만 가지고 온다 또한 +1을 써준이유도 0부터 시작하는 것을 방지하기 위해서이다.
아직 마지막에 a+b할때 ++을 씌우는 이유를 잘모르겠지만 구글링을 통해서 찾아봐야겠다.
